// Write a program that will convert a dollar amount to the number of coins that make up the amount. 
// Use the common American coins of quarters, dimes, nickels, and pennies


/*
  Input: 0.67
  Output:
  {
    quarters: 2,
    dimes: 1,
    nickels: 1,
    pennies: 2
  }
*/

function coinCounter (x) {
  // Initialize a JavaScript object to hold the coins
  var coinPurse = {}, quar = .25, dime = .10, nickel = .5, penn = .1;
	coinPurse.quarters = 0;
  coinPurse.dimes = 0;
  coinPurse.nickels = 0;
  coinPurse.pennies = 0;
	coinPurse.myFunc = function () {
		for (var i = 0, i < 100; i++) {
			if ()
		}
	};

	return coinPurse;
}

var coins = coinCounter()
console.log();